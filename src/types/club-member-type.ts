export type IClubMember = {
    id: string; // 8 chars, random string
    first: string;
    last: string;
    email: string;
    password: string;
    cell: string
    rating: number;
    status: boolean;
}